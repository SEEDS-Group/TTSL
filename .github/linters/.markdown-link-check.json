{
    "ignorePatterns": [
        {
            "pattern": "^(http|https)://localhost"
        },
        {
            "pattern": "^#"
        },
        {
            "pattern": "^https://github.com/TTSL/DSL/blob/main"
        }
    ]
}
