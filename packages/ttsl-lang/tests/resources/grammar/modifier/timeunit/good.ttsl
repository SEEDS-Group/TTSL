# $TEST$ no_syntax_error

# Function definition:
function per day f(): Int {}

# Data definition:
data per month salary: Int;

function test() {
    var x = per month f();
    var y= per week salary;
}