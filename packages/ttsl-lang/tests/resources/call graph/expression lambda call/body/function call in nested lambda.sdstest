package tests.callGraph.expressionLambdaCall.body.functionCallInNestedLambda

@Pure fun f()

function myFunction () {
    val lambda = () -> () -> f();

    # $TEST$ ["$expressionLambda"]
    »lambda()«;
}
