package tests.callGraph.expressionLambdaCall.argument.function

@Pure fun f()

function myFunction () {
    val lambda = (param: () -> ()) ->
        param();

    # $TEST$ ["$expressionLambda", "f"]
    »lambda(f)«;
}
