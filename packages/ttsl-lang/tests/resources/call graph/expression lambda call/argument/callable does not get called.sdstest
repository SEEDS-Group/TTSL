package tests.callGraph.expressionLambdaCall.argument.callableDoesNotGetCalled

@Pure fun f()

function myFunction () {
    val lambda = (param: () -> ()) -> 1;

    # $TEST$ ["$expressionLambda"]
    »lambda(f)«;
}
