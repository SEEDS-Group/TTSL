package tests.callGraph.expressionLambdaCall.defaultValue.callable

@Pure fun default()

function myFunction () {
    val lambda = (
        f: () -> () = default
    ) -> f();

    # $TEST$ ["$expressionLambda", "default"]
    »lambda()«;
}
