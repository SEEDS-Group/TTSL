package tests.callGraph.segmentCall.defaultValue.call

@Pure fun default() -> r: Any

segment mySegment(
    f: Any = default()
) {}

function myFunction () {
    # $TEST$ ["mySegment", "default"]
    »mySegment()«;
}
