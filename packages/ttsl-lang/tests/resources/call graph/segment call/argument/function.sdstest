package tests.callGraph.segmentCall.argument.function

@Pure fun f()

segment mySegment(param: () -> ()) {
    param();
}

function myFunction () {
    # $TEST$ ["mySegment", "f"]
    »mySegment(f)«;
}
