package tests.callGraph.blockLambdaCall.defaultValue.callable

@Pure fun default()

function myFunction () {
    val lambda = (
        f: () -> () = default
    ) {
        f();
    };

    # $TEST$ ["$blockLambda", "default"]
    »lambda()«;
}
