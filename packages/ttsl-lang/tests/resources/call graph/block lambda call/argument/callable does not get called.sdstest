package tests.callGraph.blockLambdaCall.argument.callableDoesNotGetCalled

@Pure fun f()

function myFunction () {
    val lambda = (param: () -> ()) {};

    # $TEST$ ["$blockLambda"]
    »lambda(f)«;
}
