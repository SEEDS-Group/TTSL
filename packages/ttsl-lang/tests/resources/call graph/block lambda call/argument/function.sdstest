package tests.callGraph.blockLambdaCall.argument.function

@Pure fun f()

function myFunction () {
    val lambda = (param: () -> ()) {
        param();
    };

    # $TEST$ ["$blockLambda", "f"]
    »lambda(f)«;
}
